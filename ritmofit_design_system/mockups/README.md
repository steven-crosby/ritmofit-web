# Mockups

Open `index.html` to browse the standalone Ritmo Studio reference screens.

All pages consume:

- `theme.css` for tokens, components, layouts, responsive behavior, focus, and reduced motion.
- `app.js` for small progressive interactions such as Library selection and pressed-state demos.

No remote imagery or framework is required. The pages are intentionally static product references,
not API-connected prototypes.
