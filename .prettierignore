# Build output and deps
node_modules
dist
build
pnpm-lock.yaml

# Generated artifacts — owned by their generators, not Prettier. Formatting these
# would fight the OpenAPI/Drizzle generators (e.g. the openapi.json drift gate).
**/src/styles/tokens.css
**/src/styles/fonts.css
apps/web/public/fonts/
apps/api/openapi/
apps/api/migrations/

# Long-form reference docs (planning + design system) — authored prose, not
# formatter-owned. Code and top-level docs (README, REVIEW, AGENTS, CLAUDE) are.
ritmofit_dev_plan/
ritmofit_design_system/
design-system-audits/
.claude/
agent-prompts/

# Agent-authored design-audit deliverables (prose, prototype HTML/CSS/JS, and
# screenshots produced by `agent-prompts/design-audit/`). Owned by their run, not
# by Prettier — an audit run must not be able to redden the repo format gate.
docs/audits/

# Vendored read-only snapshot of ritmofit-ios source (cross-surface parity prompts).
# Foreign content; refreshed verbatim from ritmofit-ios, never formatter-owned.
ios-snapshot/
